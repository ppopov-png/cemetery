# Grave detector training

This directory documents the future `grave_object` detector training pipeline. No model weights are committed here.

The browser expects the exported model at:

`web/public/models/grave-detector/grave-detector.onnx`

The annotation target is the complete visually coherent grave object selected for tracking. Do not annotate arbitrary central regions.
