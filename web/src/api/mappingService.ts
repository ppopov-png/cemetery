export type MappingTelemetry = { sessionId: string; status: string; frame: number; features: number; matches: number; mapPoints: number; pose: { x: number; y: number; z: number } }
export async function startMapping(apiUrl: string) { const response = await fetch(`${apiUrl.replace(/\/$/, '')}/api/mapping/start`, { method: 'POST' }); if (!response.ok) throw new Error(`Mapping start failed: HTTP ${response.status}`); return response.json() as Promise<{ sessionId: string }> }
export async function sendMappingFrame(apiUrl: string, sessionId: string, blob: Blob) { const form = new FormData(); form.append('frame', blob, 'frame.jpg'); const response = await fetch(`${apiUrl.replace(/\/$/, '')}/api/mapping/${sessionId}/frame`, { method: 'POST', body: form }); if (!response.ok) throw new Error(`Mapping frame failed: HTTP ${response.status}`); return response.json() as Promise<MappingTelemetry> }
export async function stopMapping(apiUrl: string, sessionId: string) { const response = await fetch(`${apiUrl.replace(/\/$/, '')}/api/mapping/${sessionId}/stop`, { method: 'POST' }); if (!response.ok) throw new Error(`Mapping stop failed: HTTP ${response.status}`); return response.json() as Promise<MappingTelemetry> }
